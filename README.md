# NLP-Driven-Content-Based-Movie-Recommendation-System

## 📌 Project Overview
This project implements a **content-based movie recommendation system** using **Natural Language Processing (NLP)** techniques.  
The system recommends movies similar to a given movie based on metadata such as **genres, keywords, cast, crew, and overview**.

It uses **text vectorization** and **cosine similarity** to identify and recommend the most relevant movies.
